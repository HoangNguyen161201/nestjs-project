# Use postgres/example user/password credentials
version: '3.1'

services:

  db:
    image: postgres
    ports:
      - '5432:5432'
    restart: always
    env_file:
      - 'docker.env'

  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080